import React, { memo } from 'react'
import type { FC, ReactNode } from 'react'
interface Props {
  children?: ReactNode
}
const Dashboardt: FC<Props> = (props) => {
  return <div>11111111111111111111</div>
}
export default memo(Dashboardt)
